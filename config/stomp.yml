default: &default
  host: <%= ENV['STOMP_HOST'] %>
  port: <%= ENV['STOMP_PORT'] %>
  export_jobs_queue: /queue/plastron.jobs
  export_jobs_completed_queue: /queue/plastron.jobs.completed

development:
  <<: *default

production:
  <<: *default

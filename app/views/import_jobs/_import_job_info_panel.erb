<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Job Info</h3>
  </div>
  <div class="panel-body">
    <p>
      <strong><%= t('activerecord.attributes.import_job.id') %>:</strong>
      <%= import_job.id %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.name') %>:</strong>
      <%= import_job.name %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.model') %>:</strong>
      <%= import_job.model %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.access') %>:</strong>
      <%= import_job.access %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.collection') %>:</strong>
      <%= import_job.collection %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.cas_user') %>:</strong>
      <%= import_job.cas_user&.name %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.timestamp') %>:</strong>
      <%= import_job.timestamp %>
    </p>

    <p>
      <strong><%= t('activerecord.attributes.import_job.file_to_upload') %>:</strong>
      <%= import_job.file_to_upload.filename %>
    </p>

    <% if import_job.binary_zip_file.attached? %>
      <p>
        <strong><%= t('activerecord.attributes.import_job.binary_zip_file') %>:</strong>
        <%= import_job.binary_zip_file.filename %>
      </p>
    <% end %>
    <% if import_job.remote_server.present? %>
      <p>
        <strong><%= t('activerecord.attributes.import_job.remote_server') %>:</strong>
        <%= import_job.remote_server %>
      </p>
    <% end %>
  </div>
</div>
